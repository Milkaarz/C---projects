#include <iostream>
#include <cmath>

// some "cmath" library commands 

int main(){

    double x = 3;
    double y = 2;
    double z = 5;
    double c = 3.14;
    double v = 3.99;

    //z = std::max(x, y); //maior que 
    //z = std::min(x, y); // menor que
    //z = pow(2, 3); // elavado a 
    //z = sqrt(9); // raiz de 
    //z = abs(-3); //numero absoluto
    //z = round (c); // arredonda o numero
    //z = ceil(c); // arredonda o numero para cima
    //z = floor(v); // arredonda para baixo
 
    std::cout << z;

    return 0;

}